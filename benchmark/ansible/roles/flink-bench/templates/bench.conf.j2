benchmark {
  manager-hostname = {{ groups['manager'][0] }}
  manager-port = 6123
  source-port = 12345
  sink-port = 12346
  bench-hostname = {{ inventory_hostname }}
  jars = ["{{ bench_dir }}/bench.jar"]
  input-path = {{ bench_dir }}/wiki.xml
  limit = 10000,
  parallelism = 10,
  rate = 20
}
