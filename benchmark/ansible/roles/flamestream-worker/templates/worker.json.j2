{
  "id": "{{ inventory_hostname }}",
  "localAddress": "{{ inventory_hostname }}:{{ flamestream_worker_port }}",
  "snapshotPath": "{{ flamestream_dir }}/data",
  "zkString": "{{ groups['manager'][0] }}:{{ zookeeper_client_port }}"
}